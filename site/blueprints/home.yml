title: Home

pages: false

fields:
  title:
    label: Title
    type:  text

  featuredarticle:
    label: Featured Article
    type: select
    search: true
    options: query
    query:
      page: /
      fetch: pages
      template: article
      flip: true
