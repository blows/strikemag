title: Home

icon: home

pages: false

fields:
  title:
    label: Title
    type:  text

  featuredarticle:
    label: Featured Post
    type: select
    search: true
    options: query
    query:
      page: /
      fetch: pages
      template: article
      flip: true

  # feature1:
  #   title: Featured Post
